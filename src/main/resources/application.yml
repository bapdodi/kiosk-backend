spring:
  datasource:
    url: jdbc:postgresql://43.201.18.30:5432/kiosk
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.tool.schema: DEBUG

erp:
  datasource:
    url: ${ERP_DB_URL:jdbc:sqlserver://43.201.18.30:1433;databaseName=DR_ERP;trustServerCertificate=true}
    username: ${ERP_DB_USERNAME:sa}
    password: ${ERP_DB_PASSWORD:1111}